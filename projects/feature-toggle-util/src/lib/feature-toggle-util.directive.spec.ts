import { FeatureToggleUtilDirective } from './feature-toggle-util.directive';

describe('FeatureToggleUtilDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureToggleUtilDirective();
    expect(directive).toBeTruthy();
  });
});
